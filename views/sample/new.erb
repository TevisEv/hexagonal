<h1>Crear Nueva Muestra</h1>
<form action="/samples" method="post">
  <p>Nombre de la muestra:</p>
  <input type="text" name="name" required>

  <p>Descripción:</p>
  <textarea name="description" required></textarea>

  <p>Propietario (Cliente):</p>
  <select name="owner_id">
    <% @clients.each do |client| %>
      <option value="<%= client.id %>"><%= client.email %></option>
    <% end %>
  </select>

  <p>Creado por (Laboratorista):</p>
  <p><%= @current_user&.dig(:email) || 'Desconocido' %></p>

  <input type="submit" value="Crear Muestra">
</form>

<p><a href="/samples">Volver a la lista de muestras</a></p>
